# 图片生成比例/分辨率与模型适配方案梳理

## 背景与目标
- 入口：剧本结构页 `ProfessionalEditor` 的图片生成。
- 模型来源：页面右上角 **AI 配置**（图片配置），不新增页面内的模型选择入口。
- 需求：根据当前图片模型，展示对应的 **比例 + 分辨率** 可选项，并在生成请求中带上参数。
- 比例清单（10 个）：
  - 1:1（正方形）
  - 2:3（竖向 2:3）
  - 3:2（横向 3:2）
  - 3:4（竖向 3:4）
  - 4:3（标准 4:3）
  - 4:5（竖向 4:5）
  - 5:4（横向 5:4）
  - 9:16（手机宽屏）
  - 16:9（电脑宽屏）
  - 21:9（超宽屏）
- 分辨率清单（Gemini 模型重点）：1K / 2K / 4K（`gemini-3-pro-image-preview`）。

## 现状梳理
- 后端 `GenerateImageRequest` 已支持 `size/width/height`，但前端未在 `ProfessionalEditor` 中提供 UI。
- 图片客户端能力差异：
  - Gemini：当前实现为把 `size` 拼到 prompt（非结构化尺寸参数）。
  - OpenAI：仅支持固定尺寸（1:1/16:9/9:16 的常见尺寸）。
  - VolcEngine（Seedream）：支持 `size`（1K/2K），不一定支持比例。
- `ProfessionalEditor` 当前没有图片模型选择器，模型统一由 AI 配置决定。

## 方案（已确认方向）
**方案一：前端按 AI 配置决定选项（无额外模型选择 UI）**
- 从 `aiAPI.list('image')` 读取启用配置，按优先级选择“当前图片模型”。
- 基于模型能力映射，决定比例 + 分辨率选项。
- 生成时带 `model`，并由前端将“比例 + 分辨率”转换为 `size/width/height` 传给后端。

## 模型能力映射建议
- `gemini-3-pro-image-preview`
  - 比例：10 种全开
  - 分辨率：1K/2K/4K
- `dall-e-3` / `dall-e-2`
  - 比例：1:1、16:9、9:16
  - 分辨率：隐藏（模型固定尺寸）
- `doubao-seedream-*`（VolcEngine）
  - 分辨率：1K/2K
  - 比例：建议仅 1:1，或提示“比例不保证”
- 其他模型：回退到 1:1 + 1K

## 交互与状态
- 不新增模型选择下拉；模型由 AI 配置决定。
- AI 配置更新时重新加载可选项。
- 生成请求按“当前页面展示的配置”发出。

## 待确认事项
1) 1K/2K/4K 的基准：按短边还是长边？
2) 是否仅在 `ProfessionalEditor` 生效，还是要同步到其它图片生成入口？

